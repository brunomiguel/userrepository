declare type RuleAttacher = (rules: string) => () => void;
/**
 * Imediately attaches CSS rules into the style sheet.
 *
 * @returns a callback to detach the rules
 */
export declare const attachRules: RuleAttacher;
export {};
