import { Event } from '@sentry/types';
/** SDK version used in every event. */
export declare const SDK_VERSION: string;
export declare const SDK_NAME = "sentry.javascript.electron";
/** Merges the given event payload with SDK defaults. */
export declare function addEventDefaults(appName: string | undefined, event: Event): Promise<Event>;
//# sourceMappingURL=context.d.ts.map