/**
 * Electron >=8.4 | >=9.1 | >=10
 * Use `render-process-gone` rather than `crashed`
 */
export declare function supportsRenderProcessGone(): boolean;
/**
 * Electron < 9 requires `crashReporter.start()` in the renderer
 */
export declare function requiresNativeHandlerRenderer(): boolean;
/**
 * Electron >= 6 uses crashpad on Windows
 */
export declare function supportsCrashpadOnWindows(): boolean;
/**
 * Electron >= 9 supports `app.getPath('crashDumps')` rather than
 * `crashReporter.getCrashesDirectory()`
 */
export declare function supportsGetPathCrashDumps(): boolean;
//# sourceMappingURL=electron-version.d.ts.map