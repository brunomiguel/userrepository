import { SegmentResolvable, SegmentUUID } from '../types/segment/Segment';
import { UserStats } from '../types/stats/UserStat';
import { VideoID, VideoResolvable } from '../types/Video';
export declare function resolveSegment(resolvable: SegmentResolvable): SegmentUUID;
export declare function resolveVideo(resolvable: VideoResolvable): VideoID;
export declare function dbuserStatsToUserStats(dbuserStat: {
    userNames: string[];
    viewCounts: number[];
    totalSubmissions: number[];
    minutesSaved: number[];
}): UserStats[];
export declare function statusCheck(res: Response): void;
