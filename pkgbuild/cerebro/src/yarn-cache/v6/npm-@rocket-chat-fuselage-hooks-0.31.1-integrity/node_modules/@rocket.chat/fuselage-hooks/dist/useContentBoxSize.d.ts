import { RefObject } from 'react';
export declare const useContentBoxSize: (ref: RefObject<HTMLElement>, options?: {
    debounceDelay?: number;
}) => Readonly<{
    inlineSize: number;
    blockSize: number;
}>;
