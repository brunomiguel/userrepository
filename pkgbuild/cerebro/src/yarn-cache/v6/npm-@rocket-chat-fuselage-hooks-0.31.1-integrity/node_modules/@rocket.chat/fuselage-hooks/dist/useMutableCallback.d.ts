/**
 * Hook to create a stable callback from a mutable one.
 *
 * @param fn - the mutable callback
 * @returns a stable callback
 * @public
 */
export declare const useMutableCallback: <P extends any[], T>(fn: (...args: P) => T) => (...args: P) => T;
