import * as React from 'react';
import { Size } from './types';
export declare function renderWithSize<T, P extends T = T>(reducer: (size: Size | null) => T, renderFunction: (props: P) => React.ReactNode): (props: Pick<P, Exclude<keyof P, keyof T>>) => JSX.Element;
